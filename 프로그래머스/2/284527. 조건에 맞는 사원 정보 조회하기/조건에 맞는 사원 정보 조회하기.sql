SELECT SUM(C.SCORE) AS SCORE, C.EMP_NO AS EMP_NO, B.EMP_NAME AS EMP_NAME, B.POSITION AS POSITION, B.EMAIL AS EMAIL FROM HR_EMPLOYEES B INNER JOIN HR_GRADE C ON B.EMP_NO = C.EMP_NO WHERE C.YEAR = 2022 GROUP BY C.EMP_NO ORDER BY SUM(C.SCORE) DESC LIMIT 1